git checkout master &&
git pull &&
git checkout mv/bugFix &&
git merge master &&
git checkout mv/refactor &&
git merge master &&
git checkout mv/features &&
git merge master &&
git checkout mv/optimizeForTesting &&
git merge master &&
git checkout mv/cleanup &&
git merge master &&
git checkout master